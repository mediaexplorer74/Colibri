<Page
    x:Class="Colibri.Shell"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:Colibri.Controls"
    mc:Ignorable="d"
    x:Name="rootElement">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <!-- subtle backdrop area -->
        <Grid x:Name="BackgroundArtGrid" Opacity="0.15" IsHitTestVisible="False">
            <Rectangle Fill="{ThemeResource SystemControlAcrylicWindowBrush}" />
        </Grid>

        <SplitView x:Name="SplitView" IsPaneOpen="True" DisplayMode="CompactInline" CompactPaneLength="48" OpenPaneLength="220">
            <SplitView.Pane>
                <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
                    <StackPanel>
                        <TextBlock Text="Colibri" FontSize="18" FontWeight="SemiBold" Margin="16,0,16,8"/>
                        <ListView x:Name="MenuList" SelectionMode="Single" Margin="0,8,0,0" SelectionChanged="MenuList_SelectionChanged">
                            <ListViewItem Tag="News"><TextBlock Text="Новости / Лента"/></ListViewItem>
                            <ListViewItem Tag="Dialogs"><TextBlock Text="Беседы"/></ListViewItem>
                            <ListViewItem Tag="Music"><TextBlock Text="Музыка"/></ListViewItem>
                            <ListViewItem Tag="Video"><TextBlock Text="Видео"/></ListViewItem>
                            <ListViewItem Tag="Settings"><TextBlock Text="Настройки"/></ListViewItem>
                        </ListView>
                    </StackPanel>
                </Grid>
            </SplitView.Pane>

            <SplitView.Content>
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <!-- Always-visible hamburger button over content -->
                    <Button x:Name="MenuButton" Content="☰" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="8" Click="MenuButton_Click"/>
                    <Frame x:Name="ContentFrame" Grid.Row="1" Margin="0,40,0,64" />
                    <controls:PlayerMiniControl Grid.Row="2"/>
                </Grid>
            </SplitView.Content>
        </SplitView>
    </Grid>
</Page>
